identidock80:
    build: .
    expose:
        - "9090"
    ports:
        - "5000:5000"
    environment:
        - ENV=prod
    volumes:
        - ./app:/app
    links:
        - dnmonster
        - redis

dnmonster:
    image: amouat/dnmonster:1.0

redis:
    image: redis:3.0
